import { Link } from "react-router-dom";

const NotFoundPage = () => {
  return (
    <div>
      NotFound component
      <Link to="/">Go to home</Link>
    </div>
  );
};

export default NotFoundPage;
